"""Pure, dependency-free modeling core.

Every module here uses only the Python standard library so the heart of the
system — the win-probability map, the de-vig math, the calibration guardrails,
and line shopping — can be run and tested with zero installation.
"""
